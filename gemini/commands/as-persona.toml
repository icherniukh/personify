description = "Apply a Personify persona to the current task or thread."
prompt = "Use the as-persona skill to apply this persona overlay for the requested scope: resolve the matching pack, construct the Persona Activation Packet, and apply the requested scope after the user's task and project instructions. User arguments: {{args}}"
