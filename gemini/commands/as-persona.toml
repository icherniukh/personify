description = "Apply a Personify persona to the current task or thread."
prompt = "Use the as-persona skill to apply this persona overlay for the requested scope. Load the matching pack and construct the Persona Activation Packet before answering. User arguments: {{args}}"
