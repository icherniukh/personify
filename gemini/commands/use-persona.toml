description = "Use a Personify persona as the default voice and reasoning lens for the session."
prompt = "Use the use-persona skill to activate this persona for the session: resolve the matching pack, construct the Persona Activation Packet, and apply the requested scope after the user's task and project instructions. User arguments: {{args}}"
