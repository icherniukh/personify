description = "Use a Personify persona as the default voice and reasoning lens for the session."
prompt = "Use the use-persona skill to activate this persona for the session. Load the matching pack and construct the Persona Activation Packet before applying it. User arguments: {{args}}"
