# Personify Gemini Extension

Generated Gemini CLI extension package for `personify`.

This directory is build output. Regenerate it with:

```bash
python3 scripts/package.py build --target gemini
```
